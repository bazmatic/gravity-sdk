base:
  role: "validator"
  data_dir: "/tmp/node1/data"
  waypoint:
    from_file: "/tmp/node2/genesis/waypoint.txt"

consensus:
  safety_rules:
    backend:
      type: "on_disk_storage"
      path: /tmp/node2/data/secure_storage.json
    initial_safety_rules_config:
      from_file:
        waypoint:
          from_file: /tmp/node2/genesis/waypoint.txt
        identity_blob_path: /tmp/node2/genesis/validator-identity.yaml

validator_network:
  network_id: validator
  listen_address: "/ip4/127.0.0.1/tcp/2025"
  discovery_method: 
    file:
      path: "/tmp/node2/discovery"
      interval_secs: 3600
  mutual_authentication: true
  identity:
    type: "from_file"
    path: /tmp/node2/genesis/validator-identity.yaml

storage:
  dir: "/tmp/node2/data"

mock_db_path: "/tmp/node2/genesis/nodes_config.json"

log_file_path: "/tmp/node2/consensus_log"

inspection_service:
  port: 10001
  address: 127.0.0.1